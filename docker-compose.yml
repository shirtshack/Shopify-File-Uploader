services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./service_credentials.json:/app/credentials.json
      - ./app.py:/app/streamlit_app.py
    env_file:
      - .env
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json

